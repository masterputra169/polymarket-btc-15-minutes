[pytest]
testpaths = tests
markers =
    unit: fast, pure-logic tests (no model training)
    integration: trains a small real model (seconds, not minutes)
    contract: guards the JSON field contract consumed by the TypeScript bot
filterwarnings =
    ignore::DeprecationWarning
